# LATTICE SPACE 0.4-1 physical MkI follow-up — 2026-09-12

## Target

- Korg Nu:Tekt NTS-1 digital kit, original / MkI
- Unit: LATTICE SPACE (`LatSpace`) custom `revfx`
- Exact binary SHA-256: `56fd5b263920831af7298839d742482b837408795b2f29aa0232b5ef37a80b56`
- Pre-handoff workflow: `34637349268`
- Branch: `field-0.1-0-test`

## Physical result

Literal tester report after the 0.4-1 guard/headroom diagnostic:

> “MUCH better. works great now. just some minor minor minor distortion on the top but this is great”

This follows the 0.4-0 result where modulation + SPACE produced obvious distortion across multiple modulation effects.

## Disposition

- SPACE 0.4-1 standalone / general musical result: **PASS for the reported playing test**.
- ModFX → SPACE 0.4-1 combination behavior: **PASS WITH NOTE**; the previous broad distortion problem is materially improved and the tester reports the combination now works great.
- Residual issue: **very minor distortion remains near the top end** of the tested range.
- 0.4-1 becomes the current MkI SPACE reference.
- Do not reopen CORE 0.3-1 based on SPACE behavior; CORE standalone and CORE + FIELD already passed separately.
- No claim is made that every modulation processor, every control position, or a long-duration soak was covered unless explicitly reported.

## Engineering interpretation

The physical improvement is consistent with the 0.4-1 changes being directionally correct: removing the sample-by-sample output guard and reducing regenerative drive while preserving readout presence materially improved real-hardware combination behavior.

It does not prove which individual change was causal, nor does it establish measured MkI CPU/deadline margin.

The remaining top-end distortion is small enough to classify as a non-blocking refinement rather than a redesign trigger. Preserve the 0.4-1 sound unless later testing shows the residual distortion is musically problematic.

## Current recommendation

Freeze SPACE 0.4-1 as the MkI reference for now. Any future cleanup should be deliberately minimal and should not trade away the musical result that passed this test.
