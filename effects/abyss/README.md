# ABYSS

Large modulated FDN reverb for the original NTS-1 MkI (`revfx`). TIME = **DECAY**; DEPTH = **SPACE**; SHIFT+DEPTH = **MIX**. Four delay lines use a Hadamard-style feedback matrix and slow unequal modulation to avoid a single metallic loop identity.
