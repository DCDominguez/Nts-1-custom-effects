# NEBULA Spec
Four independently phased sin² grains, deterministic random delay positions, shared stereo SDRAM history, bounded feedback. TIME expands the history region sampled; DEPTH raises active grain count, scatter and feedback; SHIFT_DEPTH wet/dry. Hardware QA must characterize density, grain clicks, CPU headroom and tail stability.
