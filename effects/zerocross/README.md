# ZEROCROSS

Through-zero flanger for NTS-1 MkI (`modfx`). TIME = **RATE**; DEPTH = **SWEEP**. A delayed dry reference allows the modulated tap to move from earlier to later than the reference, producing an actual zero-crossing relationship instead of a conventional one-sided flanger.

Status: M1 source; CI and MkI QA required.
