# SHARD Spec
M1 dual-grain resampling shifter in a shared stereo buffer. TIME maps a continuous ratio from 0.5→1→2; DEPTH changes grain window length; two sin² windows crossfade. This first milestone prioritizes stable pitch-shift architecture over semitone calibration; exact musical interval mapping follows hardware QA.
