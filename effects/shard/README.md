# SHARD

Dual-grain pitch-field delay for NTS-1 MkI. TIME = **PITCH** (down → unity → up), DEPTH = **GRAIN** size/texture, SHIFT+DEPTH = **MIX**. Two 180°-offset grains crossfade to mask read-head resets.
