# LONGMEMORY

Delay-to-loop continuum for NTS-1 MkI. TIME = **LENGTH**; DEPTH = **MEMORY**; SHIFT+DEPTH = **MIX**. High MEMORY approaches a dark, slowly aging near-loop instead of infinite clean feedback.
