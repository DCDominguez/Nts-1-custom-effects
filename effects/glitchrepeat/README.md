# GLITCHREPEAT

Bounded probabilistic repeater. TIME = **SLICE**; DEPTH = **CHANCE**; SHIFT+DEPTH = **MIX**. At beat boundaries it may freeze a recent slice for a finite number of repeats, then always returns to live input.
