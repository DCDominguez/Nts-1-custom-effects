# GLITCHREPEAT Spec
Continuous ring capture; BPM-derived trigger boundary; deterministic probability; finite repeat count; no state can remain latched indefinitely. TIME controls slice length, DEPTH trigger probability/repeat count, SHIFT_DEPTH wet/dry.
