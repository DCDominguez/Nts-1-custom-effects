# AUREOLE Spec
Four-line FDN plus one dual-grain 2× feedback shifter. TIME sets FDN feedback decay; DEPTH raises shimmer injection and space modulation; SHIFT_DEPTH mixes wet/dry. Pitch feedback gain is deliberately capped so HALO cannot create infinite rising energy. M1 interval is fixed at an octave; musical alternatives wait for QA.
